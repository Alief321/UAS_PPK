<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.UASPPK" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/subprimary</item>
        <item name="colorOnPrimary">@color/secondary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/subsecondary</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:editTextColor">@android:color/white</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Button.primary" parent="ThemeOverlay.AppCompat">
        <item name="colorPrimary">@color/primary</item>
    </style>
    <style name="Button.delete" parent="ThemeOverlay.AppCompat">
        <item name="colorPrimary">#B2F32222</item>
    </style>
    <style name="Button.update" parent="ThemeOverlay.AppCompat">
        <item name="colorPrimary">#FF26E629</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.UASPPK" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>